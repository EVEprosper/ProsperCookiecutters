language: python
python:
  - "3.5"
  - "3.6"
  - "3.6-dev"
install: 
  - "pip install cookiecutter"
  # - "cookiecutter ProsperFlask --no-input"
  # - "virtualenv venv_flask -p python3"
  # - "source venv_flask/bin/activate && cd PROSPERFLASK__project_root && pip install . && cd .. && deactivate"
  - "cookiecutter ProsperCron --no-input"
  - "virtualenv venv_cron -p python3"
  - "source venv_cron/bin/activate && cd PROSPERCLI__project_root && pip install . && cd .. && deactivate"
script:
  - "source venv_cron/bin/activate && cd PROSPERCLI__project_root"
  - "python setup.py travis"
  - "cd .. && deactivate"